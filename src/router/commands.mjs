export function setupCommands(client, configs) {
  // Placeholder — comandos por prefixo já são registrados via systems/index.mjs
  // Este módulo pode ser expandido para slash commands futuramente
}
